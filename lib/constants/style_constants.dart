import 'package:flutter/material.dart';

double defaultRadius = 12.0;
EdgeInsets defaultPadding = const EdgeInsets.all(8.0);
BorderRadius defaultBorderRadius = BorderRadius.circular(defaultRadius);
Color? defaultGrey = Colors.grey[200];
EdgeInsetsGeometry? formFieldContentPadding = const EdgeInsets.all(20);
